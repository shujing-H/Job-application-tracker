{
  "name": "job-application-tracker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "audit:manifest": "node scripts/audit-manifest.mjs",
    "verify": "npm test && npm run build && npm run audit:manifest",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.1.3",
    "typescript": "^5.9.2",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.2",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "vitest": "^3.2.4"
  }
}
